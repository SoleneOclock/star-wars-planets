"use client";

// ce fichier est l'equivalent d'un error bundary il sera rendu au client si dan sles composants prévus y'a une erreur qui est throw

export default function ErrorPage() {
  return <main>Une erreur est survenue</main>;
}
